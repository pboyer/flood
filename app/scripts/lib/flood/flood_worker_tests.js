var sys = require('sys');
var Worker = require('webworker');

// var w = new Worker('flood_worker.js');

// w.onmessage = function(e) {
//     sys.debug('Received mesage: ' + sys.inspect(e));
//     w.terminate();
// };

// w.postMessage({ foo : 'bar' });
